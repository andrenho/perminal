// Copyright 2015 André Wagner

#include "renderer.h"

/*
void 
Renderer::Execute()
{
    while(true) {
        // TODO - inputs!

        // parse events
        if(matrix.NewEvents()) {
            for(auto const& e: matrix.Events()) {
                if(e.type == EventType::CHAR_CHANGED) {
                    SetChar(e.ch.x, e.ch.y, e.ch.ch);
                }
            }
        }
        matrix.ClearEvents();

        // refresh
        Refresh();
    }
}
*/

// vim: ts=4:sw=4:sts=4:expandtab
